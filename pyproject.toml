[tool.pyrefly]
python_interpreter = ".venv/Scripts/python.exe"
search_path = ["."]

[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
    "visual: marks tests that generate visual output (skipped in CI)",
]

